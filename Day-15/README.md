## Topics Learned
- Method Overloading
- Rules of Method Overloading
- Overloading by changing the number of parameters
- Overloading by changing parameter data types
- Overloading by changing the order of parameters
- Difference between Method Overloading and changing only the return type
- Compile-Time Polymorphism
- Method selection by the Java compiler
- Math.pow() method
- Menu-driven programs using Method Overloading

## Practice
- Created `calculate(int a, int b)` for addition
- Created `calculate(int a, int b, int c)` for multiplication
- Created `calculate(double a, double b)` for average
- Created `calculate(int a, double b)` using `Math.pow()`
- Built a Smart Calculator using Method Overloading
- Used `Scanner` for user input
- Used `switch` statement for menu selection

## Output
- Addition of two integers
- Multiplication of three integers
- Average of two double values
- Power calculation using `Math.pow()`
- Menu-driven Smart Calculator

## Status
✅ Day 15 Completed